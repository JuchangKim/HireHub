#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
<<<<<<<< HEAD:client/node_modules/.bin/node--test
  exec "$basedir/node"  "$basedir/../test/bin/node--test.js" "$@"
else 
  exec node  "$basedir/../test/bin/node--test.js" "$@"
========
  exec "$basedir/node"  "$basedir/../atob/bin/atob.js" "$@"
else 
  exec node  "$basedir/../atob/bin/atob.js" "$@"
>>>>>>>> 327ade07a98c6260a2a2e8c184923453c911f21d:client/node_modules/.bin/atob
fi
