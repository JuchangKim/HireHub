#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:client/node_modules/.bin/node--test.ps1
    $input | & "$basedir/node$exe"  "$basedir/../test/bin/node--test.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../test/bin/node--test.js" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../atob/bin/atob.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../atob/bin/atob.js" $args
>>>>>>>> 327ade07a98c6260a2a2e8c184923453c911f21d:client/node_modules/.bin/atob.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:client/node_modules/.bin/node--test.ps1
    $input | & "node$exe"  "$basedir/../test/bin/node--test.js" $args
  } else {
    & "node$exe"  "$basedir/../test/bin/node--test.js" $args
========
    $input | & "node$exe"  "$basedir/../atob/bin/atob.js" $args
  } else {
    & "node$exe"  "$basedir/../atob/bin/atob.js" $args
>>>>>>>> 327ade07a98c6260a2a2e8c184923453c911f21d:client/node_modules/.bin/atob.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
